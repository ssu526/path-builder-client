export enum PROGRESS {
  IN_PROGRESS = "in progress",
  PENDING = "pending",
  COMPLETED = "completed",
  TERMINATED = "terminated",
}

export enum VISIBILITY {
  PUBLIC = "public",
  PRIVATE = "private",
}
